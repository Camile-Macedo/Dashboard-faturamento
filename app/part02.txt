hover{border-color:var(--accent);}
  .kpi::before{content:'';position:absolute;top:0;left:0;bottom:0;width:3px;background:var(--accent);opacity:.9;}
  .kpi:nth-child(3)::before{background:var(--grande);}
  #heatHighlightsRow .kpi:nth-child(3)::before{background:var(--accent);}
  #heatHighlightsRow .kpi:nth-child(4)::before{background:var(--grande);}
  .obs-btn{
    display:inline-flex;align-items:center;gap:5px;border:1px solid var(--border);background:var(--surface-soft);
    color:var(--text-secondary);padding:4px 9px;border-radius:6px;font-size:10.5px;font-weight:600;
    cursor:pointer;font-family:'Inter',sans-serif;margin-bottom:10px;transition:all .12s;
  }
  .obs-btn:hover{border-color:var(--accent);color:var(--num);}
  .obs-btn.open{background:var(--accent);color:#fff;border-color:var(--accent);}
  .obs-btn.has-pending{border-color:rgba(209,75,75,.5);}
  .obs-badge{
    display:inline-flex;align-items:center;justify-content:center;min-width:16px;height:16px;padding:0 4px;
    border-radius:20px;background:var(--grande);color:#fff;font-size:10px;font-weight:700;line-height:1;
  }
  .obs-btn.open .obs-badge{background:rgba(255,255,255,.3);}
  #diarioObsPanel{margin-top:-8px;margin-bottom:14px;}
  #diarioObsPanel:empty{display:none;}
  .obs-panel{background:var(--surface);border:1px solid var(--border);border-radius:11px;padding:16px 18px;box-shadow:var(--shadow-sm);}
  .obs-panel .op-title{font-size:12.5px;font-weight:600;margin-bottom:2px;display:flex;align-items:center;gap:7px;}
  .obs-panel .op-desc{font-size:11.5px;color:var(--text-secondary);margin-bottom:12px;}
  .obs-panel .op-row{display:grid;grid-template-columns:110px 70px 70px 1fr;gap:10px;align-items:center;padding:7px 0;border-top:1px solid var(--border-soft);}
  .obs-panel .op-row:first-of-type{border-top:none;}
  .obs-panel .op-date{font-family:'IBM Plex Mono',monospace;font-size:12px;color:var(--text-primary);}
  .obs-panel .op-qtd{font-family:'IBM Plex Mono',monospace;font-size:12px;color:var(--grande);text-align:right;}
  .obs-panel .op-pct{font-family:'IBM Plex Mono',monospace;font-size:11px;color:var(--text-muted);text-align:right;}
  .kpi .label{font-size:10px;color:var(--text-muted);text-transform:uppercase;letter-spacing:.05em;margin-bottom:9px;font-weight:700;}
  .kpi .value{font-family:'IBM Plex Mono',monospace;font-size:25px;font-weight:600;color:var(--text-primary);line-height:1;font-variant-numeric:tabular-nums;letter-spacing:-0.02em;}
  .kpi-tempo-detail{display:flex;flex-direction:column;}
  .kpi-tempo-rows{display:flex;flex-direction:column;gap:5px;margin-top:6px;}
  .kpi-tempo-row{display:flex;flex-direction:column;gap:3px;font-size:12px;padding:2px 0;}
  .kpi-tempo-main{display:flex;align-items:center;gap:7px;width:100%;}
  .kpi-tempo-row .l{color:var(--text-secondary);flex:1;}
  .kpi-tempo-row .v{font-family:'IBM Plex Mono',monospace;font-weight:600;color:var(--text-primary);}
  .kpi-tempo-meta{margin-left:15px;font-size:10.5px;color:var(--text-muted);line-height:1.35;}
  .kpi-tempo-bandlist{display:grid;gap:6px;margin:7px 0 0 15px;padding-top:8px;border-top:1px dashed var(--border-soft);}
  .kpi-tempo-band{display:grid;grid-template-columns:1fr auto auto;gap:8px;align-items:center;font-size:11px;}
  .kpi-tempo-band-l{color:var(--text-secondary);}
  .kpi-tempo-band-m{color:var(--text-muted);font-family:'IBM Plex Mono',monospace;font-size:10.5px;}
  .kpi-tempo-band-v{color:var(--text-primary);font-family:'IBM Plex Mono',monospace;font-weight:600;}
  .kpi-tempo-note{margin:7px 0 0 15px;font-size:10.5px;color:var(--text-muted);line-height:1.4;}
  .kpi-inline{display:flex;align-items:baseline;justify-content:space-between;gap:8px;margin-top:8px;padding-top:8px;border-top:1px solid var(--border-soft);font-size:11.5px;}
  .kpi-inline .l{color:var(--text-muted);}
  .kpi-inline .v{font-family:'IBM Plex Mono',monospace;font-weight:600;color:var(--text-primary);}
  .kpi .sub{font-size:11px;color:var(--text-muted);margin-top:5px;}
  .kpi .trend{display:inline-flex;align-items:center;gap:3px;font-size:11px;font-weight:600;margin-top:6px;font-family:'IBM Plex Mono',monospace;}
.kpi-total-compact{display:flex;flex-direction:column;justify-content:space-between;}
  .kpi-inline-box{margin-top:14px;padding-top:12px;border-top:1px solid var(--border-soft);display:grid;grid-template-columns:1fr;gap:4px;}
  .kpi-inline-box .mini-label{font-size:10px;color:var(--text-muted);text-transform:uppercase;letter-spacing:.05em;font-weight:700;}
  .kpi-inline-box .mini-value{font-family:'IBM Plex Mono',monospace;font-size:20px;font-weight:600;line-height:1;color:var(--text-primary);}
  .kpi-inline-box .mini-sub{font-size:11px;color:var(--text-muted);}
  .kpi-inline-box .trend{margin-top:4px;}
  .kpi-card-hidden{display:none!important;}
  .trend.good{color:var(--success);}
  .trend.bad{color:var(--grande);}
  .trend.flat{color:var(--text-muted);}

  .highlights{display:flex;flex-direction:column;gap:9px;}
  .highlight-item{
    display:flex;align-items:center;gap:11px;padding:10px 12px;border-radius:9px;
    background:var(--surface-soft);
  }
  .highlight-icon{
    width:30px;height:30px;border-radius:8px;flex-shrink:0;
    display:flex;align-items:center;justify-content:center;font-size:14px;
    background:var(--accent-soft);
  }
  .highlight-text{font-size:12px;color:var(--text-secondary);line-height:1.4;}
  .highlight-text b{color:var(--text-primary);font-weight:600;}

  .filial-split{display:flex;flex-direction:column;gap:12px;justify-content:center;height:100%;}
  .card-compact{padding:16px 18px;}
  .card-compact h2{font-size:13px;}
  .card-compact .desc{margin-bottom:12px;}
  .filial-row{display:flex;flex-direction:column;gap:6px;}
  .filial-row .label{display:flex;justify-content:space-between;font-size:12.5px;font-weight:500;}
  .filial-row .label .v{font-family:'IBM Plex Mono',monospace;color:var(--text-secondary);}
  .filial-row .track{height:10px;border-radius:6px;background:var(--surface-soft);overflow:hidden;}
  .filial-row .fill{height:100%;border-radius:6px;}

  .heatmap-table{border-collapse:separate;border-spacing:3px;font-size:11px;}
  .heatmap-table th{font-weight:500;color:var(--text-muted);font-size:10.5px;text-align:center;padding:2px 4px;}
  .heatmap-table td.hcell{
    width:56px;height:38px;border-radius:4px;text-align:center;vertical-align:middle;
    font-family:'IBM Plex Mono',monospace;font-size:11px;cursor:default;color:#fff;
    transition:transform .1s; line-height:1.25;
  }
  .heatmap-table td.hcell .hc-qtd{font-weight:600;}
  .heatmap-table td.hcell .hc-tempo{font-size:9px;opacity:.85;display:block;}
  .heatmap-table td.hcell:hover{transform:scale(1.15);position:relative;z-index:2;box-shadow:0 2px 8px rgba(0,0,0,.25);}
  .heatmap-table td.hcell.empty{background:var(--surface-soft);color:var(--text-muted);}
  .heatmap-table th.daylabel{text-align:right;padding-right:8px;color:var(--text-secondary);font-weight:600;}
  .heat-scale{display:flex;align-items:center;gap:4px;}
  .heat-scale .box{width:16px;height:12px;border-radius:3px;}

  .metas-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:16px;}

  .diario-goals-row{display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:18px;}
  .diario-goal-item{display:flex;flex-direction:column;gap:6px;}
  #diarioSummaryRow .kpi::before, #pessoasMesSummaryRow .kpi::before{background:var(--kpi-accent, var(--accent));}
  .diario-goal-item label{font-size:11px;color:var(--text-secondary);font-weight:600;}
  .diario-goal-item .swatch-dot{width:8px;height:8px;border-radius:50%;display:inline-block;margin-right:5px;}
  .diario-goal-readonly{
    border:1px solid var(--border-soft);border-radius:7px;padding:7px 10px;font-size:13px;width:100%;max-width:150px;
    font-family:'IBM Plex Mono',monospace;background:var(--surface-soft);color:var(--text-secondary);
  }

  .ranking-card{margin-bottom:16px;overflow:hidden;}
  .ranking-header{display:flex;justify-content:space-between;align-items:flex-start;gap:18px;flex-wrap:wrap;margin-bottom:16px;}
  .ranking-header h2{font-size:15px;margin:0 0 4px;}
  .ranking-header .desc{margin:0;max-width:780px;line-height:1.5;}
  .ranking-formula{display:flex;gap:6px;flex-wrap:wrap;align-items:center;}
  .ranking-weight{display:inline-flex;align-items:center;gap:5px;padding:5px 8px;border:1px solid var(--border);border-radius:20px;background:var(--surface-soft);font-size:10.5px;color:var(--text-secondary);white-space:nowrap;}
  .ranking-weight b{font-family:'IBM Plex Mono',monospace;color:var(--text-primary);}
  .ranking-score-guide{margin:-2px 0 16px;padding:12px 14px;border:1px solid rgba(124,58,237,.28);border-radius:10px;background:rgba(124,58,237,.06);}
  .ranking-score-guide-title{font-size:11.5px;font-weight:700;color:var(--text-primary);margin-bottom:4px;}
  .ranking-score-guide-text{font-size:10.5px;color:var(--text-secondary);line-height:1.5;}
  .ranking-score-bands{display:flex;gap:7px;flex-wrap:wrap;margin-top:9px;}
  .ranking-score-band{padding:4px 8px;border-radius:999px;background:var(--surface);border:1px solid var(--border);font-size:9.5px;color:var(--text-secondary);}
  .ranking-score-band b{color:var(--text-primary);font-family:'IBM Plex Mono',monospace;}
  .ranking-podium{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;margin-bottom:16px;}
  .ranking-podium-item{position:relative;border:1px solid var(--border);border-radius:10px;background:var(--surface-soft);padding:14px 14px 13px;min-width:0;}
  .ranking-podium-item.first{border-color:rgba(217,154,43,.5);background:rgba(217,154,43,.07);}
  .ranking-podium-item.second{border-color:rgba(135,146,160,.45);}
  .ranking-podium-item.third{border-color:rgba(190,120,55,.4);}
  .ranking-pos{font-family:'IBM Plex Mono',monospace;font-size:11px;font-weight:700;color:var(--text-muted);margin-bottom:7px;}
  .ranking-name{font-size:13px;font-weight:650;color:var(--text-primary);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
  .ranking-score{font-family:'IBM Plex Mono',monospace;font-size:25px;font-weight:650;line-height:1;margin:9px 0 5px;color:var(--text-primary);}
  .ranking-score small{font-family:'Inter',sans-serif;font-size:10px;color:var(--text-muted);font-weight:600;margin-left:3px;}
  .ranking-podium-meta{font-size:10.5px;color:var(--text-secondary);line-height:1.45;}
  .score-pill{display:inline-flex;align-items:center;justify-content:center;min-width:48px;padding:4px 8px;border-radius:20px;font-family:'IBM Plex Mono',monospace;font-size:11.5px;font-weight:700;}
  .score-pill.excellent{color:var(--success);background:rgba(34,197,94,.13);}
  .score-pill.good{color:var(--warn);background:rgba(217,154,43,.14);}
  .score-pill.attention{color:var(--grande);background:rgba(209,75,75,.12);}
  .rank-pos-cell{font-family:'IBM Plex Mono',monospace;font-weight:700;color:var(--text-secondary);}
  .rank-sub{display:block;font-size:9.5px;color:var(--text-muted);font-family:'Inter',sans-serif;font-weight:500;margin-top:2px;white-space:nowrap;}
  .rank-bar{width:92px;height:6px;border-radius:6px;background:var(--surface-soft);overflow:hidden;margin-left:auto;}
  .rank-bar > span{display:block;height:100%;border-radius:6px;background:var(--accent);}
  .ranking-note{margin-top:11px;padding:9px 11px;border-radius:8px;background:var(--surface-soft);font-size:10.5px;color:var(--text-secondary);line-height:1.5;}
  .ranking-note b{color:var(--text-primary);}
  .ranking-empty{padding:26px 14px;text-align:center;color:var(--text-muted);font-size:12px;border:1px dashed var(--border);border-radius:9px;}
  #fair-ranking-table th{white-space:nowrap;}
  .category-base-card{margin:-2px 0 16px;padding:12px 14px;border:1px solid var(--border-soft);border-radius:10px;background:var(--surface-soft);}
  .category-base-title{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:9px;font-size:11px;color:var(--text-secondary);}
  .category-base-title b{color:var(--text-primary);font-size:11.5px;}
  .category-base-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:7px;}
  .category-base-item{position:relative;overflow:hidden;padding:9px 10px 8px;border:1px solid var(--border);border-radius:8px;background:var(--surface);min-width:0;}
  .category-base-item::before{content:'';position:absolute;left:0;right:0;top:0;height:3px;background:var(--cat-color);}
  .category-base-name{font-size:11px;font-weight:700;color:var(--text-primary);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
  .category-base-range{font-family:'IBM Plex Mono',monospace;font-size:10px;color:var(--text-muted);margin-top:4px;}
  .ranking-detail-panel{margin:0 0 16px;padding:14px;border:1px solid var(--border);border-radius:10px;background:var(--surface-soft);}
  .ranking-detail-panel[hidden]{display:none;}
  .ranking-detail-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:12px;}
  .ranking-detail-head h3{margin:0 0 3px;font-size:14px;}
  .ranking-detail-head p{margin:0;color:var(--text-muted);font-size:10.5px;}
  .ranking-detail-summary{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:8px;margin-bottom:12px;}
  .ranking-detail-summary-item{padding:9px 10px;border:1px solid var(--border);border-radius:8px;background:var(--surface);min-width:0;}
  .ranking-detail-summary-label{font-size:9px;color:var(--text-muted);text-transform:uppercase;letter-spacing:.03em;}
  .ranking-detail-summary-value{font-family:'IBM Plex Mono',monospace;font-size:13px;font-weight:650;color:var(--text-primary);margin-top:4px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
  .ranking-detail-summary-sub{font-size:9px;color:var(--text-muted);margin-top:2px;}
  .ranking-detail-close{border:0;background:transparent;color:var(--text-muted);font-size:18px;cursor:pointer;line-height:1;padding:2px 4px;}
  .ranking-dist-bar{height:12px;display:flex;overflow:hidden;border-radius:999px;background:var(--border-soft);margin-bottom:12px;}
  .ranking-dist-seg{height:100%;min-width:0;transition:width .2s ease;}
  .ranking-category-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px;}
  .ranking-category-card{border:1px solid var(--border);border-radius:9px;background:var(--surface);padding:10px;min-width:0;}
  .ranking-category-top{display:flex;align-items:center;gap:7px;margin-bottom:8px;}
  .ranking-category-dot{width:8px;height:8px;border-radius:50%;flex:0 0 auto;}
  .ranking-category-name{font-size:11px;font-weight:700;color:var(--text-primary);}
  .ranking-category-range{font-size:9.5px;color:var(--text-muted);margin-left:auto;white-space:nowrap;}
  .ranking-category-main{font-family:'IBM Plex Mono',monospace;font-size:20px;font-weight:650;color:var(--text-primary);line-height:1;}
  .ranking-category-main small{font-family:'Inter',sans-serif;font-size:9.5px;color:var(--text-muted);font-weight:600;margin-left:3px;}
  .ranking-category-stats{display:grid;grid-template-columns:1f